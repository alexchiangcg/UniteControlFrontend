services:
  react-app:
    container_name: uniteSlave-frontend
    build:
      context: .
      dockerfile: Dockerfile
    image: uniteslave-frontend
    ports:
      - "8080:80"
    restart: unless-stopped
